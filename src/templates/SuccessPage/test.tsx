import 'match-media-mock'

import SuccessPage from '.'
import { render } from 'utils/test-utils'

describe('<SuccessPage />', () => {
  it('should render the SuccessPage', () => {
    // render(<SuccessPage />)
  })
})
